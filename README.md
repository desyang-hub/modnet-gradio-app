### Modnet模型Gradio快捷部署，space在线体验

#### 环境安装
```bash
$ pip install -r requirements.txt
```

#### 启动服务
```bash
$ python app.py
```

#### Docker部署

构建镜像
```bash
docker compose up --build
```

启动服务
```bash
docker compose up -d
```